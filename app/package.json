{
    "name": "asr-label",
    "version": "0.1.0",
    "private": true,
    "bin": "server.js",
    "scripts": {
        "build": "cp -rf ../out . && pkg ."
    },
    "dependencies": {
        "express": "^4.18.2",
        "open": "^8.4.2"
    },
    "pkg": {
        "scripts": "server.js",
        "assets": "./out/**/*",
        "targets": [
            "node18-macos-arm64",
            "node18-win-x64",
            "node18-linux-x64"
        ],
        "outputPath": "dist"
    }
}
